include: package:lints/recommended.yaml

analyzer:
  exclude:
    - lib/.generated/**

linter:
  rules:
    # React component functions intentionally use component-style names.
    non_constant_identifier_names: false
