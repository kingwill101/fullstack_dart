name: newsletter
description: A new Routed application.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  args: ^2.5.0
  artisanal: ^0.5.0
  liquify: ^1.6.1
  routed: ^0.5.1
  routed_core: ">=0.5.0 <1.0.0"
  routed_views: ">=0.2.0 <1.0.0"
  stem: ^0.3.0
  stem_sqlite: ^0.2.0

dev_dependencies:
  lints: ^6.0.0
  property_testing: ^0.3.2+1
  routed_cli: ^0.4.0
  routed_testing: ">=0.4.0 <1.0.0"
  server_testing: ^0.4.0
  stem_cli: ^0.2.0
  test: ^1.26.3

dependency_overrides:
  artisanal: ^0.5.0
